site_name: Total Recall
site_description: Cross-session, cross-CLI memory for Claude Code and other AI coding assistants — mines your session transcripts so new sessions know your decisions, corrections, bans, and goals.
site_url: https://88plug.github.io/total-recall/
repo_url: https://github.com/88plug/total-recall
repo_name: 88plug/total-recall
edit_uri: edit/main/docs/
use_directory_urls: true

theme:
  name: material
  features:
    - navigation.sections   # group nav by section, uses width well
    - navigation.top        # back-to-top button
    - navigation.instant    # SPA-style fast page loads
    - content.code.copy     # copy button on code blocks (needs superfences below)
    - search.suggest
  palette:
    # Light/dark toggle that follows the OS preference by default.
    - media: "(prefers-color-scheme: light)"
      scheme: default
      toggle:
        icon: material/weather-night
        name: Switch to dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      toggle:
        icon: material/weather-sunny
        name: Switch to light mode

markdown_extensions:
  - admonition
  - attr_list
  - md_in_html
  - def_list
  - footnotes
  - tables
  - pymdownx.highlight     # code highlighting (ships with mkdocs-material)
  - pymdownx.superfences   # required for content.code.copy + nested fences
  - pymdownx.details       # collapsible <details> admonitions
  - toc:
      permalink: true
